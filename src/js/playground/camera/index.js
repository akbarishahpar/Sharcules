var Camera = /** @class */ (function () {
    function Camera(app, width, height) {
        this.app = app;
        this.width = width;
        this.height = height;
        this.x = 0;
        this.y = 0;
    }
    Camera.prototype.transition = function () { };
    return Camera;
}());
export {};
